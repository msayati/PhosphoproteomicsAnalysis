library("WGCNA")
library(tidyr)


naive_bayes <- function(S, A){
  #S -> [.8, .9, .3]
  #A -> [.3, .4, .5, .4, .2, .... ]
  sharedLength <- length(S)
  
  for(psite in unknownSites){
    for(k in S){
      
    }
  }
  
  
} 

